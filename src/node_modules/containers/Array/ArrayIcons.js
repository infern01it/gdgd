import React from 'react';

import classNames from 'classnames';

const ArrayIcons = ({infrastructure}) => {
  return (
    <section className="page-array__icons parr-icons">
      <div className="container">
        <div className="row">
          {
            typeof infrastructure === 'object' && infrastructure.map((element, id) => {
              const colClass = classNames(
                'col-sm-6 col-lg-5',
                {
                  'offset-lg-1': id % 2 === 1
                }
              );
              return (
                <div
                  key={id}
                  className={colClass}
                >
                  <div className="parr-icons__item card-arr-icon">
                    <img
                      src={element.img}
                      className="img-svg card-arr-icon__icon"
                    />
                    <h4 className="card-arr-icon__title">{element.title}</h4>
                    <div className="card-arr-icon__desc b-content">
                      <p>{element.desc}</p>
                    </div>
                  </div>
                </div>
              );
            })
          }
        </div>
      </div>
    </section>
  );
}

export default ArrayIcons;