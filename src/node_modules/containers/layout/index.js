import React from 'react';

const Layout = ({children}) => (
	<div>
		<header>
			<nav>
				<span>Home</span>
				<span>Article</span>
			</nav>
		</header>

		<main>
			{children}
		</main>
	</div>
);

export default Layout;