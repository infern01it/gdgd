const DefaultValueSityRender = (selected, options) => {
	if (selected.length === 0) {
		return 'Выберите город...';
	}

	if (selected.length === options.length) {
		return 'Выбраны все города';
	}

	// if(selected.length !== 0 && selected.length <= 2) {
	// 	const select = options.filter(el => {
	// 		return el.value === selected[0] || el.value === selected[1];
	// 	});
	// 	return select.length !== 2 ? select[0] : `${select[0]}, ${select[1]}`;
	// }

	return `Выбрано ${selected.length} города`;
}

export default DefaultValueSityRender;