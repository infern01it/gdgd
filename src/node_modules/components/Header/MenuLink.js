import React from 'react';

const MenuLink = ({isMobile, thisPage, link1, link2}) => {
  if( !thisPage || !isMobile ) {
    return link1;
  }
  if( thisPage && isMobile ) {
    return link2;
  }
}

export default MenuLink;