export const renderPrice = price => {
  if( price >= 1 ) {
    return `${price} млн. руб.`
  } else {
    return `${price*1000} тыс. руб.`
  }
}

export const renderArea = area => {
  if( area >= 1 ) {
    return `${area} гектар`;
  } else {
    return `${area * 100} соток`;
  }
}