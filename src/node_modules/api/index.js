import phones from './mockPhones';
import arrays from './mockArrays';

export const fetchPhones = async () => {
	return new Promise(resolve => {
		resolve(phones);
	});
}

export const fetchArrays = async () => {
	return new Promise(resolve => {
		resolve(arrays);
	});
}